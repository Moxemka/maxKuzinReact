import { Paper, styled } from "@mui/material";

const PaperMd3 = styled(Paper)(({ theme }) => ({
    borderRadius: '15px',
    paddingTop: '5px',
    paddingBottom: '5px'
  }));

export default PaperMd3;